{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: latex-postgres-initdb
  namespace: {{ .Values.namespace }}
data:
  init.sql: |-
{{ .Files.Get "files/postgres/init.sql" | indent 4 }}
{{- end }}
