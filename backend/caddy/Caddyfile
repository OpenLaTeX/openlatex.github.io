# (site pas encore déployé, le serveur doit être on) 
http://openlatex.blavogiez.fr, http://openlatex-prod.blavogiez.fr, http://openlatex-dev.blavogiez.fr {
    handle /collab/* {
        reverse_proxy collab:7000
    }
    handle /compile {
        # rediriger vers l'ip du node qui a l'ingress kubernetes (Cest un path type exact et pas prefix)
        reverse_proxy 172.16.10.52:80
    }
    # pas accessible
    handle /metrics {
        respond 403
    }
    handle /grafana* {
        reverse_proxy grafana:3000
    }
    @frontend path / /assets/*
    handle @frontend {
        reverse_proxy frontend:80
    }
    handle {
        reverse_proxy backend:8000
    }
}
